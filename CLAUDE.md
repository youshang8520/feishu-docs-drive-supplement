## Project Isolation Rules (auto-generated)
- 仅在“开始新项目 / 切换到新的项目目录”时启用隔离规则。
- 在当前项目内做配置修复、联调、排障时，不强制隔离，以避免上下文中断。
- 未经用户明确指令，不使用与当前任务无关的 skill。
- 默认仅访问当前项目目录；若任务明确需要（如 memory/hooks 调试），可访问用户已授权目录。
- push / PR 操作前必须确认；其他本地可逆操作可直接执行。
